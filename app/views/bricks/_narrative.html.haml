%div{class: ['ui',content.widened_class,'wide','column', ('segment' if editing)] }
  .ui.text
    .p 
      = markdown(content.configuration["body"])
  = render partial: 'bricks/settings', locals: { content: content } if current_page?(:controller => 'features', :action => 'edit')